# Flint ProGuard/R8 rules. Add keep rules here as the app grows.
# AccessibilityService + foreground services are referenced from the manifest, so the
# manifest merger keeps them; no extra rules needed yet.
